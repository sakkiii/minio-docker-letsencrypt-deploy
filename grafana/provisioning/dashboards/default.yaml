apiVersion: 1

providers:
  - name: alphaban.ml - Dashboards
    folder: '' # The folder where to place the dashboards
    type: file
    disableDeletion: false
    allowUiUpdates: true
    editable: true  
    options:
      path: /etc/grafana/dashboards
